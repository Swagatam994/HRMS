# Setup

## Environment files
- Copy Backend/.env.example to Backend/.env and update values locally.
- Copy Frontend/.env.example to Frontend/.env and update values locally.

## Local run
- Backend: cd Backend && npm install && npm run dev
- Frontend: cd Frontend && npm install && npm run dev

## Git
- Local environment files are ignored by Git.
- Example env files are tracked so collaborators can copy them.
